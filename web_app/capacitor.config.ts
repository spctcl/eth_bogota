import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.spctcl.MonserrAuth',
  appName: 'MonserrAuth',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
