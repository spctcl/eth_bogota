import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.monserrauth.app',
  appName: 'monserrauth',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
